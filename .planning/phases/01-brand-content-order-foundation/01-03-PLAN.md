---
phase: 01-brand-content-order-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/ui/button.tsx
  - src/components/public/header.tsx
  - src/components/public/footer.tsx
  - src/components/public/mobile-menu.tsx
autonomous: true
requirements: [BRAND-01, BRAND-02, ORDER-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every public route receives the same responsive Header, main landmark, and concise Footer."
    - "Desktop exposes only Home and the distinct order action; mobile offers an accessible compact panel while keeping brand and order action prominent."
    - "Every shared primary order action continues to use the centrally configured safe Facebook Page handoff."
  artifacts:
    - path: src/components/public/header.tsx
      provides: "Server-rendered public navigation and primary order action."
    - path: src/components/public/footer.tsx
      provides: "Concise brand/current-year/footer order action."
    - path: src/components/public/mobile-menu.tsx
      provides: "The sole client-side mobile navigation state leaf."
  key_links:
    - from: src/app/layout.tsx
      to: src/components/public/header.tsx
      via: "root shell composition"
    - from: src/components/public/header.tsx
      to: src/components/public/mobile-menu.tsx
      via: "responsive mobile navigation leaf"
    - from: src/components/public/header.tsx
      to: src/components/public/order-cta.tsx
      via: "shared configured primary CTA"
---

<objective>
Expand the tracer into the responsive, consistent shared public shell that downstream routes will inherit.

Purpose: Make the verified conversion action and boutique identity consistent across the site without exposing unshipped destinations.
Output: Header, Footer, compact accessible mobile menu, and finalized shared token/button behavior wired through the root layout.
</objective>

<execution_context>
@/Users/jerumgalang/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/jerumgalang/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md
@.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md
@.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md
@.planning/phases/01-brand-content-order-foundation/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `src/components/public/header.tsx` — `Header` server public navigation.
- `src/components/public/footer.tsx` — `Footer` concise public footer.
- `src/components/public/mobile-menu.tsx` — `MobileMenu` stateful accessible menu panel.
- `src/app/layout.tsx` — final shared Header/main/Footer composition.
- `src/app/globals.css` — global token, focus, and reduced-motion refinements.
- `src/components/ui/button.tsx` — semantic tactile variants/sizes reused by the public shell.

<tasks>

<task type="auto">
  <name>Task 1: Compose the responsive server-rendered public shell</name>
  <read_first>@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md (D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11); @.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md (Design System, Spacing, Typography, Color, Primary-Screen Hierarchy); @.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md (header, footer, layout, globals, button assignments); @node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md; @node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md; @node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md; @node_modules/next/dist/docs/01-app/01-getting-started/11-css.md; @node_modules/next/dist/docs/03-architecture/accessibility.md; src/app/layout.tsx; src/app/globals.css; src/components/ui/button.tsx; src/components/public/order-cta.tsx</read_first>
  <files>src/components/public/header.tsx, src/components/public/footer.tsx, src/app/layout.tsx, src/app/globals.css, src/components/ui/button.tsx</files>
  <action>Create server-rendered Header and Footer components, then compose them around the root main landmark. Per D-08, D-09, D-10, and D-11, Header exposes only a `next/link` Home destination plus the dedicated `OrderCta` primary action, while Footer contains only a concise brand note, runtime current-year treatment, and the same action; do not publish future routes, sitemap entries, or unapproved contacts. Use the supplied shadcn Base Nova `Button` and `cn` helper rather than adding a UI dependency or parallel button system; refine its semantic-token variants and a 44px control size for the tactile/focus/pressed states required by D-07. Per D-04, D-05, and D-06, finalize globals for the documented 4px-based spacing, exact font roles, palette roles, fine borders/organic texture, visible focus, and `prefers-reduced-motion`; preserve text wrapping and accessible contrast. Keep Header/Footer/layout server components and avoid placing public content facts or Facebook URL literals in these components. Explicit probe assumptions: responsive palette, shell consistency, and layout overflow are judged by build plus required desktop/mobile manual inspection because no browser test harness belongs in this phase.</action>
  <verify><automated>npm run lint && npm run build</automated><human-check>Run `npm run dev`; inspect the shared shell at desktop and 320px widths. Confirm Header/Footer contain only the permitted destinations, the order action is visually distinct, focused controls are visible, text wraps, and no horizontal viewport scrolling occurs.</human-check></verify>
  <acceptance_criteria>The root shell consistently renders Header, main, and Footer; its navigation scope is limited to Home and the configured order action; controls satisfy the documented visual, focus, touch-target, and motion behavior.</acceptance_criteria>
  <done>Any current or future public route rendered inside the root layout inherits the same server-rendered boutique shell and centralized order path.</done>
</task>

<task type="auto">
  <name>Task 2: Add the narrow accessible mobile-navigation interaction leaf</name>
  <read_first>@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md (D-08, D-09, D-10); @.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md (navigation, overflow, interactive-control considerations); @.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md (mobile-menu assignment and server/client boundary); @node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md; @node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md; @node_modules/next/dist/docs/03-architecture/accessibility.md; src/components/public/header.tsx; src/components/public/order-cta.tsx; src/components/ui/button.tsx</read_first>
  <files>src/components/public/mobile-menu.tsx, src/components/public/header.tsx</files>
  <behavior>
    - The native menu trigger exposes an accessible name and reflects open state with `aria-expanded`.
    - Activating the trigger opens/closes the compact panel; Escape closes it; selecting Home closes it.
    - The panel offers only Home and the reusable configured order action, while the root layout remains server-rendered.
  </behavior>
  <action>Create `MobileMenu` as the sole `"use client"` leaf, with local state and a Lucide trigger icon only where it improves recognition. Per D-10, retain a prominent brand and order action at narrow widths while the compact panel provides only the permitted Home destination and imported `OrderCta`. Use native button/link semantics, a state-aware accessible trigger name, `aria-expanded`, Escape close behavior, Home-activation close behavior, and visible focus; avoid focus trapping, arbitrary portal logic, a menu package, clientifying the layout, or importing the catalog. Integrate the leaf into Header's responsive composition. Verify the documented keyboard transitions through the required manual 320px keyboard review; do not introduce a browser test harness in this phase.</action>
  <verify><automated>npm run lint && npm run build</automated><human-check>At 320px, use only the keyboard: tab to the menu trigger, open it, verify `aria-expanded` changes in browser accessibility inspection, close with Escape, reopen it, activate Home, and confirm the panel closes with visible focus throughout.</human-check></verify>
  <acceptance_criteria>The mobile menu is the only client component in the shared shell, exposes its state semantically, supports its documented keyboard transitions, and contains no destination beyond Home and the central order action.</acceptance_criteria>
  <done>Visitors on narrow screens can access the truthful public navigation without losing the prominent order action or server-rendered shell architecture.</done>
</task>

</tasks>

<backstops>
  <backstop category="long-text" requirement="BRAND-02">At 320px, wordmark, CTA, support text, and footer content wrap without clipping or horizontal page overflow.</backstop>
  <backstop category="keyboard-navigation" requirement="BRAND-02">The compact menu preserves visible focus and supports trigger, Escape-close, and Home-activation-close behavior.</backstop>
</backstops>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor interaction → mobile navigation state | Untrusted browser events operate the local open/close state. |
| shared shell → Facebook Page | Every public-shell CTA hands the visitor to the configured external service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | Header/Footer CTA use | high | mitigate | Both compose `OrderCta`; no component owns an external destination literal. |
| T-01-08 | Elevation of privilege | mobile/menu external tab | medium | mitigate | `OrderCta` retains safe new-tab relations and the menu has no dynamic destinations. |
| T-01-09 | Denial of service | mobile menu interaction | low | mitigate | Local bounded boolean state, native controls, and no remote/menu API calls. |
| T-01-SC | Tampering | package installs | high | accept | No package-manager installation occurs in this plan. |
</threat_model>

<verification>
Run `npm run lint && npm run build`; complete the desktop/mobile visual and keyboard checklist for shared shell behavior.
</verification>

<success_criteria>
The final Phase 1 shell is responsive and consistent, exposes only truthful current navigation, and routes every primary public order action through the single safe Facebook Page CTA.
</success_criteria>

<output>
Create `.planning/phases/01-brand-content-order-foundation/01-03-SUMMARY.md` when done.
</output>
