---
phase: 01-brand-content-order-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-brand-content-order-foundation/01-SKELETON.md
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/public/order-cta.tsx
  - src/content/business.ts
autonomous: true
requirements: [BRAND-01, BRAND-02, DATA-01, DATA-03, ORDER-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A visitor can load Home and use its primary order action to open the configured Facebook Page in a safe new tab."
    - "The first public screen uses the approved bakery palette, serif display/sans body pairing, and truthful missing-fact treatment."
  artifacts:
    - path: src/content/business.ts
      provides: "The only Facebook Page URL and exact public CTA copy."
    - path: src/components/public/order-cta.tsx
      provides: "Reusable safe external order control."
    - path: .planning/phases/01-brand-content-order-foundation/01-SKELETON.md
      provides: "Phase-one architectural contract preserving the local-content/no-database boundary."
  key_links:
    - from: src/components/public/order-cta.tsx
      to: src/content/business.ts
      via: "imported configured href, label, and adjacent support text"
    - from: src/app/page.tsx
      to: src/components/public/order-cta.tsx
      via: "Home route renders the reusable conversion control"
---

<objective>
Establish the production-quality public tracer: a visitor reaches the Home route and begins a truthful Facebook-based order inquiry.

Purpose: Prove the local-content-to-public-route-to-external-order path before expanding the shell and catalog.
Output: The walking-skeleton contract, centralized business configuration, reusable CTA, and an initial branded Home route.

MVP framing: **As a** visitor, **I want to** reach Antigua's verified Facebook order destination from Home, **so that** I can begin an inquiry without being misled about the handoff.
</objective>

<execution_context>
@/Users/jerumgalang/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/jerumgalang/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md
@.planning/phases/01-brand-content-order-foundation/01-RESEARCH.md
@.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md
</context>

## Artifacts this phase produces

- `.planning/phases/01-brand-content-order-foundation/01-SKELETON.md` — Walking Skeleton decisions and local-run contract.
- `src/content/business.ts` — `businessContent` (or equivalent immutable typed business/order export), with the configured Page URL, CTA label, warning, and explicit missing-fact shape.
- `src/components/public/order-cta.tsx` — `OrderCta` reusable external anchor.
- `src/app/globals.css` — semantic Antigua palette/font/base-motion tokens.
- `src/app/layout.tsx` — server-rendered document font and main-shell foundation.
- `src/app/page.tsx` — the initial public Home tracer composition.

<tasks>

<task type="tracer">
  <name>Task 1: Deliver the typed-content-to-Facebook Home tracer</name>
  <read_first>@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-12, D-13, D-14); @.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md (Design System, Copywriting Contract, Primary-Screen Hierarchy); @.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md (business, order-cta, layout, page, globals assignments); @node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md; @node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md; @node_modules/next/dist/docs/01-app/01-getting-started/11-css.md; @node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md; @node_modules/next/dist/docs/03-architecture/accessibility.md; src/app/layout.tsx; src/app/page.tsx; src/app/globals.css</read_first>
  <files>src/content/business.ts, src/components/public/order-cta.tsx, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <action>Create the single immutable typed business/order source with the Facebook Page destination, exact configured label, exact adjacent login warning, and explicit absent expected-fact representation. The Page URL literal belongs only in this module. Create `OrderCta` as a normal external anchor that imports this configuration, uses `target="_blank"` and `rel="noopener noreferrer"`, and applies the installed shadcn `buttonVariants` styling rather than another button system. Per D-12, D-13, and D-14, do not create a deep link, prefill, form/API path, or a claim that Facebook messaging occurs anonymously. Replace starter route content with a small server-rendered Home composition whose factual displays are imported from the content module and whose primary order action is `OrderCta`; include expected missing factual/image slots with the exact D-01, D-02, and D-03 policy rather than arbitrary facts, stock imagery, or component-local product copy. Per D-04, D-05, D-06, and D-07, convert global semantic variables to the five approved colors, load Playfair Display and Inter with `next/font/google`, set the specified typography/spacing roles, visible focus, tactile one-pixel press feedback, and reduced-motion handling. Keep the root layout server-rendered and provide the flex-growing main landmark; do not add a client boundary there. Explicit probe assumptions: BRAND-01, BRAND-02, DATA-01, DATA-03, and ORDER-01 have no supplied edge taxonomy, so this tracer resolves them by rendering only configuration-derived public content and compiling the route; the visual and external-navigation properties are covered by the phase manual check.</action>
  <verify><automated>npm run lint && npm run build</automated><human-check>Run `npm run dev`, visit `http://localhost:3000`, and at desktop plus 320px widths confirm the wordmark and sole filled order action lead the screen; inspect the action's href, new-tab attributes, label, and adjacent login warning; use keyboard Tab to confirm focus is conspicuous.</human-check></verify>
  <acceptance_criteria>The Home tracer compiles and renders its order action exclusively through `OrderCta`; that control receives its destination and text from one typed local module, opens the configured Facebook Page safely in a separate tab, and the initial public screen follows the approved palette, font pairing, and placeholder policy.</acceptance_criteria>
  <done>A visitor can load Home and start the configured Facebook Page handoff; no product facts, image URLs, prices, descriptions, or alternate Facebook destinations appear in route/component JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Record the deliberate static walking-skeleton contract</name>
  <read_first>@/Users/jerumgalang/.config/opencode/gsd-core/references/skeleton-template.md; @.planning/PROJECT.md (Constraints and Key Decisions); @.planning/ROADMAP.md (Phase 1 and downstream phases); @.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md (Phase Boundary and D-12, D-13, D-14)</read_first>
  <files>.planning/phases/01-brand-content-order-foundation/01-SKELETON.md</files>
  <action>Create the Walking Skeleton record using the required template. Document Next.js App Router, typed immutable local content as the genuine static read boundary, no application auth, the documented local development command, and the `src/app`/`src/components`/`src/content` layout. Explicitly record that database/CMS and a database read/write are deliberately prohibited by Phase 1 scope, not omitted accidentally; record the Page-based new-tab handoff and all deferred commerce/integration paths. This is reversible documentation of the established architecture, with the scope constraint preserved for downstream plans.</action>
  <verify><automated>test -f .planning/phases/01-brand-content-order-foundation/01-SKELETON.md && npm run lint && npm run build</automated></verify>
  <acceptance_criteria>The skeleton states the runnable user capability, records the local typed-content decision, and explicitly distinguishes prohibited data/auth/deployment additions from future work.</acceptance_criteria>
  <done>Later phases have a committed architectural contract that prevents reintroducing a database, CMS, auth path, or unverified Messenger design into the Phase 1 foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local content → public markup | Owner-approved factual data becomes public business/product content. |
| public CTA → Facebook Page | Browser leaves the site for Facebook authentication and messaging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | `src/content/business.ts` and Home | high | mitigate | Typed canonical source and locked placeholders prevent unsupported public claims. |
| T-01-02 | Tampering | `OrderCta` destination | high | mitigate | One non-input Page URL export is imported by the sole CTA component and manually inspected. |
| T-01-03 | Elevation of privilege | external browser tab | medium | mitigate | The external anchor uses `noopener noreferrer`. |
| T-01-SC | Tampering | package installs | high | accept | No package-manager installation occurs in this plan. |
</threat_model>

<verification>
Run `npm run lint && npm run build`. Then execute the specified desktop/mobile and keyboard review; Facebook authentication itself remains outside the application boundary.
</verification>

<success_criteria>
The local-configured Facebook Page handoff works end-to-end from the public Home route, and the Phase 1 static architecture is explicitly documented.
</success_criteria>

<output>
Create `.planning/phases/01-brand-content-order-foundation/01-01-SUMMARY.md` when done.
</output>
