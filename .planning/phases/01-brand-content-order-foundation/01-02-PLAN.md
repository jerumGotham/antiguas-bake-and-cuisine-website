---
phase: 01-brand-content-order-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/content/menu.ts
  - src/content/selectors.ts
  - src/components/ui/card.tsx
  - src/components/ui/section.tsx
  - src/components/ui/image-placeholder.tsx
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, BRAND-02]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Future public product displays can receive only typed records from a canonical local catalog."
    - "A missing expected image displays the branded image and factual placeholder treatment, while optional absent data can remain omitted."
  artifacts:
    - path: src/content/menu.ts
      provides: "Typed canonical categories and owner-approved product records."
    - path: src/content/selectors.ts
      provides: "Pure stable catalog selectors."
    - path: src/components/ui/image-placeholder.tsx
      provides: "Truthful approved-image-or-placeholder boundary."
  key_links:
    - from: src/content/selectors.ts
      to: src/content/menu.ts
      via: "selectors import and filter the canonical catalog without fallback records"
    - from: src/components/ui/image-placeholder.tsx
      to: src/content/menu.ts
      via: "a type-only import binds ImagePlaceholder props to the canonical Product image metadata contract; approved source and supplied alt render an image, while all other cases render the branded placeholder"
---

<objective>
Expand the tracer with a canonical product-data contract and reusable truthful display primitives.

Purpose: Downstream Home and Menu work must be unable to invent a second catalog or silently substitute unapproved media.
Output: Typed menu contracts/selectors plus server-compatible card, section, and image-placeholder primitives.
</objective>

<execution_context>
@/Users/jerumgalang/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/jerumgalang/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md
@.planning/phases/01-brand-content-order-foundation/01-RESEARCH.md
@.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md
@.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md
@.planning/phases/01-brand-content-order-foundation/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `src/content/menu.ts` — `ProductCategory`, `Product`, optional approved metadata contracts, canonical categories, and canonical products export.
- `src/content/selectors.ts` — pure category/product selector exports.
- `src/components/ui/card.tsx` — `Card` structural primitive.
- `src/components/ui/section.tsx` — `Section` semantic/gutter primitive.
- `src/components/ui/image-placeholder.tsx` — `ImagePlaceholder` approved-image display boundary.

<tasks>

<task type="auto">
  <name>Task 1: Establish canonical typed catalog contracts and deterministic selectors</name>
  <read_first>@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md (D-01, D-02, D-03); @.planning/phases/01-brand-content-order-foundation/01-RESEARCH.md (Canonical data plus approval-aware display boundary); @.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md (menu and selectors assignments); @.planning/REQUIREMENTS.md (DATA-01 through DATA-03)</read_first>
  <files>src/content/menu.ts, src/content/selectors.ts</files>
  <behavior>
    - A selector given a declared category returns only canonical products for that exact category in declaration order.
    - An empty catalog, a single product, or a category with no records produces a stable empty/single-item result without synthetic records.
    - Product optional metadata stays absent until owner approval; supplied price is a display string, avoiding numeric rounding or currency conversion.
  </behavior>
  <action>Define named strict TypeScript contracts for the approved category set and immutable product records, including optional description, image, sizes, price, and best-seller metadata. Export one canonical category collection and one canonical product collection; no placeholder product, stock-media URL, invented price, description, availability, or business claim may be used to populate it. Implement pure selectors that import this collection and preserve source order. Per D-01, D-02, and D-03, represent expected missing fields explicitly at the contract boundary so a renderer can choose the exact textual or image placeholder, and omit optional fields with no expected visual slot. Resolve DATA-02 probes defensibly: exact category equality is the literal typed category identifier (no normalization, trimming, or Unicode equivalence); equal category values remain separate records and do not merge; empty/single input keeps identity behavior; output order is declaration order; and prices are opaque approved display strings so no precision loss or tie-breaking occurs. Do not add browser state, JSX, fetching, a CMS/database, or an order mutation.</action>
  <verify><automated>npm run lint && npm run build</automated><human-check>Review the exported category and product collections with representative exact-category, empty, and single-record inputs to confirm selectors preserve declaration order and synthesize no records.</human-check></verify>
  <acceptance_criteria>Type checking accepts only the declared category/product data shapes; selectors derive results solely from the canonical export and preserve exact category matching and source ordering; unknown factual metadata has no fabricated fallback.</acceptance_criteria>
  <done>The project has one strongly typed local catalog boundary ready for owner-approved records and deterministic selectors that safely handle empty and partial data.</done>
</task>

<task type="auto">
  <name>Task 2: Provide reusable truthful product and section presentation boundaries</name>
  <read_first>@.planning/phases/01-brand-content-order-foundation/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-06, D-07); @.planning/phases/01-brand-content-order-foundation/01-UI-SPEC.md (Spacing Scale, Typography, Copywriting Contract, UI Considerations); @.planning/phases/01-brand-content-order-foundation/01-PATTERNS.md (card, section, image-placeholder assignments); @node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md; @node_modules/next/dist/docs/01-app/01-getting-started/11-css.md; @node_modules/next/dist/docs/01-app/01-getting-started/12-images.md; src/components/ui/button.tsx; src/lib/utils.ts</read_first>
  <files>src/components/ui/card.tsx, src/components/ui/section.tsx, src/components/ui/image-placeholder.tsx</files>
  <action>Create stateless, server-compatible `Card` and `Section` primitives using `cn`, semantic color tokens, fine borders, documented gutters/spacing, wrapping text, and restrained tactile feedback. Create `ImagePlaceholder` with a type-only dependency on the canonical `Product` image metadata contract from `src/content/menu.ts`: render an owner-approved local image only when its source and meaningful supplied alt are available; otherwise render the soft-beige branded treatment with D-03's image label and D-02's factual placeholder. Per D-04, D-05, D-06, and D-07, preserve the artisan aesthetic, readable font roles, visible focus, and reduced-motion compatibility without a client component. Do not accept arbitrary remote URLs, generate alt text, embed product facts, or use stock imagery. Explicit UI backstop: long supplied values and placeholders must wrap without clipping, overlap, or horizontal viewport overflow at 320px.</action>
  <verify><automated>npm run lint && npm run build</automated><human-check>At a 320px viewport, inspect a representative long placeholder/value in the primitives' consuming Home composition and confirm words wrap with no horizontal page scroll.</human-check></verify>
  <acceptance_criteria>Card and Section use reusable semantic styling; ImagePlaceholder has one approved-image path and one branded no-image path using the locked copy, with neither source fetching nor fabricated media.</acceptance_criteria>
  <done>Downstream routes can render typed product records and missing image/fact states consistently without embedding product facts in JSX.</done>
</task>

</tasks>

<backstops>
  <backstop category="long-text" requirement="DATA-03">At 320px, approved values and the placeholder treatment wrap without clipping, overlap, or horizontal page overflow.</backstop>
</backstops>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-approved records → selector/display primitives | Product facts and media eligibility become public presentation inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Spoofing | `menu.ts` records | high | mitigate | Immutable typed canonical records carry only owner-approved optional fields; unknown values remain absent. |
| T-01-05 | Tampering | `selectors.ts` | medium | mitigate | Pure exact-match selectors preserve canonical order and create no fallback data. |
| T-01-06 | Information disclosure | image display primitive | low | mitigate | The primitive accepts only approved local source metadata and never adds remote URLs or secrets. |
| T-01-SC | Tampering | package installs | high | accept | No package-manager installation occurs in this plan. |
</threat_model>

<verification>
Run `npm run lint && npm run build`; perform the 320px wrapping backstop review after a consuming route is available.
</verification>

<success_criteria>
The canonical catalog is strongly typed, deterministic under boundary/empty/order probes, and its reusable presentation primitives preserve the locked truthful-placeholder policy.
</success_criteria>

<output>
Create `.planning/phases/01-brand-content-order-foundation/01-02-SUMMARY.md` when done.
</output>
